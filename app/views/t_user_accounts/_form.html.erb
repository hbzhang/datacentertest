<%= form_for(@t_user_account) do |f| %>
  <% if @t_user_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@t_user_account.errors.count, "error") %> prohibited this t_user_account from being saved:</h2>

      <ul>
      <% @t_user_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
